/**
 * [Visual Inspector - UI 수정 지시 모드]
 * 사용자의 요청으로 배포 화면에서 버튼을 노출하지 않도록 비활성화되었습니다.
 */
interface VisualInspectorProps {
  currentScreen?: string;
}

export function VisualInspector(_props: VisualInspectorProps) {
  return null;
}
